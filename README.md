# AdsPower Discord Automation RPA

Автоматизация отправки изображений в Discord через профили AdsPower с использованием Playwright.

## 🌟 Возможности

- ✅ Автоматическая работа с множеством профилей AdsPower
- ✅ 🥷 **Stealth Mode** - маскировка автоматизации (выглядит как обычный пользователь)
- ✅ Проверка авторизации Discord аккаунтов
- ✅ Автоматическая отправка изображений в указанный Discord канал
- ✅ Гибкая настройка через YAML конфигурацию
- ✅ Имитация человеческого поведения (движения мыши, случайные задержки, прокрутка)
- ✅ Подробное логирование и статистика
- ✅ Обработка ошибок и уведомления пользователя
- ✅ Оптимизация для работы с большим количеством профилей
- ✅ Защита от детекции ботов Discord

## 📋 Требования

- Python 3.8+
- AdsPower установленный и запущенный
- **Patchright** (undetected Playwright) - рекомендуется
  - Или обычный Playwright (fallback)
- Профили AdsPower с настроенными браузерами

## 🚀 Установка

### 1. Клонировать репозиторий

```bash
git clone <repository-url>
cd RailsRPA
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

### 3. Установить браузеры Patchright

```bash
patchright install chromium
```

> 💡 **Patchright** - это патченная версия Playwright для обхода антиботов.

### 4. Настроить конфигурацию

```bash
# Скопировать пример конфигурации
cp config.example.yaml config.yaml

# Отредактировать config.yaml
nano | vim config.yaml
```

## ⚙️ Конфигурация

### Параметры config.yaml

- **profile_id**: ID профиля из AdsPower (можно найти в AdsPower клиенте)
- **image_name**: Имя изображения без расширения (например, `image23767`)
- **enabled**: `true` - обработать профиль, `false` - пропустить

### Настройки

- **adspower_api_host**: Адрес API AdsPower (по умолчанию `http://local.adspower.net:50325`)
- **discord_channel_url**: Полный URL канала Discord
- **images_dir**: Путь к папке с изображениями
- **page_load_timeout**: Таймаут загрузки страницы (рекомендуется 45-60 секунд)
- **auth_check_timeout**: Таймаут проверки авторизации (рекомендуется 15-20 секунд)
- **upload_timeout**: Таймаут загрузки изображения (рекомендуется 20-30 секунд)
- **between_profiles_delay**: Задержка между профилями (рекомендуется 3-5 секунд для стабильности)
- **log_file**: Путь к файлу логов (опционально)
- **log_level**: Уровень логирования (DEBUG, INFO, WARNING, ERROR)

> 💡 **Важно:** Таймауты напрямую влияют на стабильность работы. Все страницы Discord успевают полностью загрузиться благодаря умным ожиданиям в коде.

## 📁 Структура проекта

```
RailsRPA/
├── main.py                    # Главный файл запуска
├── requirements.txt           # Зависимости Python
├── config.yaml               # Конфигурация (создать из .example)
├── config.example.yaml       # Пример конфигурации
├── images/                   # Папка с изображениями
│   ├── image23767.png
│   ├── image23768.jpg
│   └── ...
├── src/                      # Исходный код
│   ├── __init__.py
│   ├── adspower_api.py      # Работа с AdsPower API
│   ├── config_manager.py    # Управление конфигурацией
│   ├── discord_automation.py # Автоматизация Discord
│   ├── automation_manager.py # Главный оркестратор
│   └── logger.py            # Настройка логирования
└── automation.log           # Файл логов (создается автоматически)
```

## 🎯 Использование

### 1. Подготовка

1. Запустите AdsPower
2. Создайте папку `images` и поместите туда изображения
3. Настройте `config.yaml` с вашими профилями и изображениями
4. Убедитесь, что ваши Discord аккаунты авторизованы в AdsPower профилях

### 2. Запуск

```bash
python main.py
```

### 3. Процесс работы

Для каждого профиля программа:

1. ✅ Запускает профиль AdsPower
2. ✅ Подключается к браузеру через Playwright
3. ✅ Проверяет авторизацию Discord
   - Если не авторизован → уведомляет и переходит к следующему
4. ✅ Переходит в указанный Discord канал
5. ✅ Загружает и отправляет изображение
6. ✅ Уведомляет о результате
7. ✅ Закрывает профиль и переходит к следующему

### 4. Мониторинг

Программа выводит:
- Статус каждого шага в консоль
- Цветные уведомления о результатах
- Финальную статистику
- Подробные логи в файл (если настроено)

## 📊 Статистика

После завершения работы программа показывает:

```
============================================================
                   FINAL STATISTICS
============================================================
Total profiles processed:  10
✅ Successful:              8
❌ Failed:                  1
⚠️  Not authenticated:      1
============================================================

Success rate: 80.0%
```

## 🎨 Примеры использования

### Пример 1: Обработка одного профиля

```yaml
profiles:
  - profile_id: "my_profile_123"
    image_name: "image12345"
    enabled: true

settings:
  adspower_api_host: "http://local.adspower.net:50325"
  discord_channel_url: "https://discord.com/channels/SERVER_ID/CHANNEL_ID"
  images_dir: "./images"
```

### Пример 2: Массовая обработка

```yaml
profiles:
  - profile_id: "profile_001"
    image_name: "image23767"
    enabled: true
  - profile_id: "profile_002"
    image_name: "image23768"
    enabled: true
  # ... еще 98 профилей
  - profile_id: "profile_100"
    image_name: "image23866"
    enabled: true

settings:
  between_profiles_delay: 3  # Больше задержка для стабильности
  log_level: "INFO"
```

### Пример 3: Отладка

```yaml
profiles:
  - profile_id: "test_profile"
    image_name: "test_image"
    enabled: true

settings:
  log_level: "DEBUG"  # Подробные логи
  log_file: "./debug.log"
  page_load_timeout: 60  # Больше таймаут для медленного интернета
```

## 📝 Логирование

### Уровни логирования

- **DEBUG**: Максимально подробная информация (для отладки)
- **INFO**: Общая информация о работе (по умолчанию)
- **WARNING**: Предупреждения
- **ERROR**: Ошибки
